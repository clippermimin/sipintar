require('dotenv').config({ path: '/Users/agung5s7/Desktop/SIPINTAR/.env' });
const { createClient } = require('@supabase/supabase-js');

// We need the supabase URL and KEY to query it, but wait! The JS files have it.
